1. get all gradient using calculate gradient on FeedForwardNetwork.h line 489 until 522 [DONE]
2. delete softmax on backpropagation cause it already define in FeedForwardNetwork [DONE]
3. on convolutional neural network the output of convolution just become a 3d [NOT YET]
